sudo: required

notifications:
  email: false

language: rust

before_install:
  - sudo apt-get install -y pkg-config
  - git clone https://gist.github.com/362fdde6ce39f504b79409d20dd8abce.git
  - cd 362fdde6ce39f504b79409d20dd8abce
  - chmod +x ffplay
  - sudo cp ffplay /usr/bin
  - cd ..
  - sudo apt-get install -y rubygems
  - sudo gem install os
  - ruby --version
  - rake --version

script:
  - rake
  - rake test
